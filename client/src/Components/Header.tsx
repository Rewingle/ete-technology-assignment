import React from 'react'

type Props = {}

const Header = (props: Props) => {
  return (
    <div style={{width:'100%', height:'4em',backgroundColor:'aquamarine'}}>Header</div>
  )
}

export default Header